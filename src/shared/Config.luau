--!strict
-- Movement spike only. None of these values are final economy tuning.
return table.freeze({
    Build = "food-growth-004",
    WalkSpeed = 18,
    ArenaHalfWidth = 90,
    CameraMinZoom = 12,
    CameraMaxZoom = 80,
    FoodPoints = 10,
    FoodRespawnSeconds = 8,
    FoodPickupRadius = 4,
    FoodTickSeconds = 0.1,
})
